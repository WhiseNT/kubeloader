// ============================================
// 01 · ?? 空值合并：必须响亮失败
// ============================================
// expect-error: ??
//
// Rhino 能解析 ??，但会算错它的值（右侧表达式被忽略），属于「不报错但结果错」
// ——这是最危险的失败方式。所以转换器选择硬失败，把脚本跳过并给出改写建议。
//
// 正确写法：显式判断，或用 ??=（它由转换器展开，不受此限）。

const fallback = null
const picked = fallback ?? 'default'
__check('这行不该被执行到', picked, 'never')
